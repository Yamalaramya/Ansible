- name: conditions
  hosts: local
  connection: local
  vars:
    number: 5
  tasks:
   - name: check number is less than 10 or not
     ansible.builtin.debug:
        msg: "number is {{number}} is less 10"
     when: numberv < 10
      
